const validationRules = require('./validationRules');
const userValidationResult = require('./userValidationResult');
const productValidationResult = require("./productValidationResult");

module.exports = {
    validationRules,
    userValidationResult,
    productValidationResult
};